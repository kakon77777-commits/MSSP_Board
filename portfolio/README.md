<!-- GENERATED by portfolio/render-index.mjs. Do not edit:
     portfolio/verify-portfolio.mjs exits 1 when this disagrees with its inputs. -->
# 二十個應用專案

固定二十個位置。**身分**在 `roadmap.json`，**工作**在 `products/NN-slug.json`，
而後者只有在真的有工作之後才存在——一個帶著預設欄位的佔位符，
是一份關於沒人做過的工作的宣稱。

`current_gate`、生命週期投影與 `technical_close_eligible` 都是從 stage 紀錄**推導**的，
沒有存在任何地方。一份存下來的摘要會跟它摘要的東西不一致，而那時被讀的是摘要。

結案的 commit 必須從 `main` **可達**，而且記錄的 tree 必須等於那個 commit 的 tree。
物件存在不算數：一個 fetch 下來但沒合併的 commit 也存在，而它什麼都沒證明。

**1 技術結案 · 0 進行中 · 14 已命名未開始 · 5 待審定名**

| # | 專案 | 生命週期 | 目前的關卡 | 介紹頁 |
|---:|---|---|---|---|
| 01 | **純文字編輯器** | 技術結案 | （無） | 未建立 |
| 02 | `file-manager-basic` | 未開始 | — | — |
| 03 | `markdown-editor` | 未開始 | — | — |
| 04 | `spreadsheet` | 未開始 | — | — |
| 05 | `git-client` | 未開始 | — | — |
| 06 | `email-client` | 未開始 | — | — |
| 07 | `etl-pipeline` | 未開始 | — | — |
| 08 | `chart-dashboard` | 未開始 | — | — |
| 09 | `rag-application` | 未開始 | — | — |
| 10 | `evaluation-harness` | 未開始 | — | — |
| 11 | `point-of-sale` | 未開始 | — | — |
| 12 | `accounting-ledger` | 未開始 | — | — |
| 13 | `booking-system` | 未開始 | — | — |
| 14 | `inventory-manager` | 未開始 | — | — |
| 15 | `password-manager` | 未開始 | — | — |
| 16 | *待審定名* | — | — | — |
| 17 | *待審定名* | — | — | — |
| 18 | *待審定名* | — | — | — |
| 19 | *待審定名* | — | — | — |
| 20 | *待審定名* | — | — | — |

## 01. 純文字編輯器

`text-editor-basic` · 技術結案 · 紀錄 `portfolio/products/01-text-editor-basic.json`

一個會拒絕它讀不懂的檔案的編輯器：UTF-8 以外一律具名拒絕，BOM 與行尾隨檔案往返，而它讀到的格式看得見。

| 階段 | 適用性 | 狀態 |
|---|---|---|
| `preregistration` | required | passed |
| `mssp_core` | required | passed |
| `work_package` | required | passed |
| `candidate` | required | passed |
| `island_verification` | required | passed |
| `integration` | required | passed |
| `system_acceptance` | required | passed |
| `technical_close` | required | passed |

**實際做過的工作項目**（變動長度，不是固定格）：

- `a0-shell` packaged Electron shell and security boundary — passed
- `a0-file-loop` new / open / save / Save As, unsaved-change guard — passed
- `a1-editing` undo / redo / clipboard / find / replace — passed
- `a2-encoding-boundary` injected codec, format carried whole, visibility row — passed
- `a2-outsourced-units` two units produced off-seat and integrated byte-identical — passed

**這個產品量出來的東西：**

- 一個單元的島驗證不涵蓋產品的載入語意：DMS 投影在島裡 11 向量、10/10 變異全過，用 CommonJS 編出來在真實視窗一行都跑不了。
- 跨檔案一致性要區分「原始產出」與「整合者修過的合成體」。
- cancelled 與 refused 必須是不同的狀態，而且都要帶未變的邊界——缺欄位只是沉默，不是證據。
- 一個 acceptance 因成本超標而 RED 時，先問儀器做的是不是規格說的那個動作。

```text
測試              69 條，0 條失敗
鑽孔              3 支，0 個變異活下來
驗收 ID           10 條，0 條未結
外包進樹的單元    2 個，逐位元未改
```

```text
commit  7366c4ec0e4404bbb571964adcdc139254df6c50
tree    c636d10854dd6aeade5ad3131f527421bc8036ae
結案    2026-08-30
```

席位：建置 Elenchos · manifest/oracle Metron · 系統驗收 Pragma

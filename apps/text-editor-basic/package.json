{
  "name": "text-editor-basic",
  "version": "0.0.0",
  "private": true,
  "description": "MSSP product-scale trial, app 1. Preregistration: slices/01-text-editor-basic/preregistration.json",
  "main": "dist/main/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/render-renderer.mjs",
    "start": "electron .",
    "test": "node --test tests/*.test.mjs",
    "build:check": "node scripts/render-renderer.mjs --check"
  },
  "devDependencies": {
    "electron": "^33.0.0",
    "playwright": "^1.62.1",
    "typescript": "^5.9.3"
  }
}
